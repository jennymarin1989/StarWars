import styled from 'styled-components';

const StyleTitle = styled.text`
    font-size: 0.8em;
    color: yellow;
    font-family: verdana;
    font-style: italic;
    text-align: center;
`;

export default StyleTitle;